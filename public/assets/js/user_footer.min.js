/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
/*!******************************************!*\
  !*** ./resources/assets/js/dashboard.js ***!
  \******************************************/
var contacturl = $("#url").val();
$(document).ready(function () {
  ajaxRequest('POST', "".concat(contacturl, "/user/dashboard"), null, null, null, function (result) {
    if (result.flag == 1) {
      var response = result.data;
      $('#contact_Count_id').text(response.contact_count);
      $('#group_Count_id').text(response.group_count);
    }
  });
});
$("#contact_btn").click(function (result) {
  if (result.flag == 0) {
    showToast(result.msg, 'danger');
  } else {
    window.location.href = "".concat(contacturl, "/user/contact");
  }
  ;
});
$("#group_btn").click(function (result) {
  if (result.flag == 0) {
    showToast(result.msg, 'danger');
  } else {
    window.location.href = "".concat(contacturl, "/user/group");
  }
  ;
});
})();

// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
/*!***************************************!*\
  !*** ./resources/assets/js/logout.js ***!
  \***************************************/
var url = $("#url").val();
$(document).ready(function () {
  $("#logout_btn").click(function (result) {
    ajaxRequest('POST', "".concat(url, "/logout"), null, 'application/x-www-form-urlencoded', true, function (result) {
      if (result.flag == 1) {
        window.location.href = "".concat(url);
      }
    });
  });
});
})();

/******/ })()
;